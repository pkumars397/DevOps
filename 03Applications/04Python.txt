>>python2(2000-2010)
>>python3(2008 to present)

>>yum install python3 
>>python3 -V 

Python package manager (PIP,installed when installing python)
>>pip3 -v
>>pip install flask
>>pip show flask
>>python3 -c "import sys; print(sys.path)"

>>pip install flask jinja2

>>putting all package into requirements.text
>>pip install =r requirements.txt 

>>if not mentioning the versions of the packages then most probably ,pip will install latest versions

>> pip install flask --upgrade 
>> pip uninstall flask 

#easy_install 
>>app.py >> setuptools >>app.egg(similar to jar in java)
>>easy_install install app 

#wheels 
app.py >> setuptools >>app.whl 
pip install app.whl

