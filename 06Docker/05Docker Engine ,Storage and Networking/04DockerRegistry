>> the docker hub is docker registry ...
>> pulling image,if not specified path default /library/imageName

>>image : docker.io/library/nginx 

         registry/userid/imageRepo

>> docker login ,always login if pulling or pushing from hub.

# private docker registry 
>>have to login before pull push  

# deploy private registry
>docker run -d -p 5000:5000 --name registry registry:2
>docker image tag my-image localhost:5000/my-image
>docker push localhost:5000/my-image
>docker pull localhost:5000/my-image
>docker pull 192.168.56.100:5000/my-image